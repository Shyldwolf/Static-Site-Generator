# Static Site Generator (Python)

Generates a static site from `/content` using `template.html`, copying assets and building output into `/docs`.

## Features
- Recursive page generation
- Copies `/static` into output folder
- Simple scripts to build + serve

## Run
./main.sh
# builds and serves on port 8888
